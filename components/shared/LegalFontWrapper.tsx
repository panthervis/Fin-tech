import * as React from "react"
// import cn from "classnames"

const FontWrapper: React.FunctionComponent = ({ children }) => (
  <div className="md:text-base">{children}</div>
)

export default FontWrapper
